# Характеристики нелинейности и хаотичности

Приложение для анализа временных рядов методами нелинейной динамики.

## 📁 Структура проекта

```
vkr/
├── mainwindow.py          # Главное окно приложения (рефакторенное)
├── config.py              # Конфигурация и настройки
├── utils.py               # Общие утилиты
├── ui_form.py             # Сгенерированный UI
├── form.ui                # Исходный файл UI
├── requirements.txt       # Зависимости
├── Makefile              # Автоматизация
├── README.md             # Документация
│
├── functions/            # Модули анализа
│   ├── __init__.py       # Экспорт модуля
│   ├── base.py           # Базовые классы (BaseAnalyzer, AnalysisResult)
│   │
│   ├── H_vvod.py         # Ввод данных
│   ├── Hinich_my.py      # Тест Хинича
│   ├── Herst_my.py       # Херст и Ляпунов
│   ├── Hoelder_my.py     # Показатель Гёльдера
│   ├── segment_my.py     # Сегментация
│   ├── Curth_my.py       # Куртозис и пик-фактор
│   ├── bifurk_my.py      # Бифуркация
│   ├── bifurk_my_1.py    # Кумулятивные суммы
│   ├── SSA_my.py         # SSA анализ
│   ├── specgram_my.py    # Спектрограмма
│   │
│   └── *_f.py            # Вспомогательные функции
│
├── widgets/              # UI виджеты
│   ├── __init__.py
│   └── matplotlib_widget.py  # Виджет matplotlib
│
└── matlab/               # Исходный код MATLAB (для справки)
```

## 🏗️ Архитектура

### Рефакторинг (версия 2.0)

Проект был рефакторен для улучшения архитектуры и поддерживаемости:

#### Новые модули:

1. **`config.py`** — Централизованная конфигурация:
   - Параметры по умолчанию
   - Правила валидации
   - Настройки графиков
   - Поддерживаемые форматы файлов

2. **`utils.py`** — Общие утилиты:
   - `iter_windows()` — генератор скользящих окон
   - `setup_figure()` / `finalize_figure()` — создание графиков
   - Загрузка данных, валидация, нормализация

3. **`functions/base.py`** — Базовые классы:
   - `BaseAnalyzer` — базовый класс анализатора
   - `AnalysisResult` — результат анализа

#### Улучшения:

- ✅ Централизованная конфигурация
- ✅ Единая система логгирования
- ✅ Валидация параметров и данных
- ✅ Обработка ошибок с подробными сообщениями
- ✅ Типизация (type hints)
- ✅ Документирование (docstrings)
- ✅ Обратная совместимость со старыми функциями

## 🚀 Установка

### Быстрый запуск

```bash
make install ui test-data run
```

### Пошаговая установка

```bash
# Создание виртуального окружения
python3 -m venv venv

# Активация
source venv/bin/activate  # Linux/Mac

# Установка зависимостей
pip install -r requirements.txt

# Генерация UI
pyside6-uic form.ui -o ui_form.py
```

## 📦 Сборка в бинарный файл

### Автоматическая сборка

```bash
# Установка и сборка
make install build
```

Бинарный файл создаётся в папке в зависимости от ОС:
- **Linux**: `bin/linux/vkr_analysis`
- **macOS**: `bin/macos/vkr_analysis`
- **Windows**: `bin/windows/vkr_analysis.exe`

### Команды сборки

```bash
make build      # Сборка бинарного файла
make build-clean # Очистка результатов сборки
make info       # Информация о системе
make test-data  # Создание тестовых данных (в test_data/)
```

### Ручная сборка

```bash
# Установка PyInstaller
pip install pyinstaller

# Сборка
pyinstaller build.spec

# Бинарник будет в папке bin/<os>/
```

## 📋 Использование

### Запуск приложения

```bash
make run
# или
./venv/bin/python mainwindow.py
```

### Параметры по умолчанию

| Параметр | Описание | Значение |
|----------|----------|----------|
| window_width | Ширина окна | 200 |
| start_offset | Начало отсчёта | 1 |
| window_shift | Сдвиг окна | 100 |
| num_bands | Число полос | 30 |
| deviation | Отклонение | 4.0 |
| num_windows | Число окон | 200 |
| smoothing_alpha | Коэффициент сглаживания | 0.01 |
| jump_number | Номер скачка | 1 |

### Методы анализа

1. **📊 Ввод данных** — Загрузка временного ряда
2. **📈 Тест Хинича** — Проверка гауссовости
3. **📊 Херст и Ляпунов** — Персистентность и хаотичность
4. **📐 Показатель Гёльдера** — Гладкость функции
5. **✂️ Сегментация** — Поиск скачков скорости
6. **📉 Куртозис и пик-ф-р** — Эксцесс и пики
7. **🔄 n-я бифуркация** — Фазовые плоскости
8. **📊 Кумулятивные суммы** — Детектирование изменений
9. **🌊 Колебания (SSA)** — Выделение компонент
10. **📊 Спектрограмма** — Частотно-временной анализ

## 🔧 Разработка

### Команды Makefile

```bash
make install   # Установка зависимостей
make ui        # Генерация UI файлов
make run       # Запуск приложения
make test-data # Создание тестовых данных
make test      # Тестирование функций
make clean     # Очистка
make deps      # Проверка зависимостей
make help      # Справка
```

### Добавление новой функции анализа

1. Создайте файл в `functions/`:

```python
from .base import BaseAnalyzer, AnalysisResult
from utils import iter_windows, setup_figure, finalize_figure

class MyAnalyzer(BaseAnalyzer):
    def __init__(self):
        super().__init__("MyAnalyzer")
    
    def analyze(self, data, **kwargs) -> AnalysisResult:
        if not self.validate_data(data):
            return AnalysisResult()
        
        # Ваш код анализа
        result = self._compute(data, **kwargs)
        
        return self._create_result(data=result, metric_name="value")
```

2. Добавьте импорт в `functions/__init__.py`

3. Добавьте обработчик в `mainwindow.py`

### Логгирование

```python
import logging
logger = logging.getLogger(__name__)

logger.debug("Отладочное сообщение")
logger.info("Информационное сообщение")
logger.warning("Предупреждение")
logger.error("Ошибка")
```

## 📊 Тестовые данные

Для тестирования создайте данные:

```bash
make test-data
```

Будут созданы файлы:
- `test_sinusoid.txt` — синусоида с шумом
- `test_random_walk.txt` — случайное блуждание
- `test_jumps.txt` — сигнал со скачками
- `test_short.txt` — короткий сигнал
- `test_multifreq.txt` — мультичастотный сигнал

## 📝 Лицензия

Проект создан в рамках выпускной квалификационной работы.

## 👥 Авторы

- Порт с MATLAB: оригинальные функции
- Рефакторинг и архитектура: версия 2.0
